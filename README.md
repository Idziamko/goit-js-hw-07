# GoIT JavaScript Homework 06

Homework for Module 6: "Object Oriented Programming (OOP)".

## 📂 Task Description

This project demonstrates the use of the `this` keyword, Classes, and Private
properties:

1.  **task-1.js** — `customer` Object: Refactoring object methods to correctly
    use the `this` keyword for accessing properties (`balance`, `discount`,
    `orders`).
2.  **task-2.js** — `Storage` Class: Creates a class to manage a warehouse
    inventory. Uses a private property `#items` and methods to add, remove, and
    retrieve items.
3.  **task-3.js** — `StringBuilder` Class: Creates a class for string
    manipulation. Uses a private property `#value` and methods to pad the string
    at the start, end, or both sides.

## 🚀 How to Verify

1. Open the GitHub Pages link (or `index.html` locally).
2. Open Developer Tools (`F12`).
3. Go to the **Console** tab.
4. Verify that the outputs match the expected results for all tasks.

## 🛠 Tech Stack

- **HTML5**
- **JavaScript** (ES6+, Classes, Private fields `#`)
