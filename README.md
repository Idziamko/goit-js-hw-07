# GoIT JavaScript Homework 07

Homework for Module 7: "The Document Object Model (DOM) and Events".

## 📂 Task Description

This project demonstrates DOM manipulation and event handling in JavaScript:

* **task-1.js — Categories:** Traversing the DOM tree using `querySelectorAll` and `forEach` to count categories and their nested elements.
* **task-2.js — Image Gallery:** Dynamically creating HTML markup for an image gallery from an array of objects and inserting it into the DOM using `insertAdjacentHTML`.
* **task-3.js — Input Event:** Handling the `input` event to update a text greeting in real-time based on user input.
* **task-4.js — Login Form:** processing a form `submit` event, preventing page reload, creating an object with form data, and validating inputs.
* **task-5.js — Background Color Switcher:** Changing the `<body>` background color via inline styles upon a button `click` event using a random hex color generator.

## 🚀 How to Verify

1.  Open the GitHub Pages link (or `index.html` locally).
2.  Open **Developer Tools** 
3.  Go to the **Console** tab to verify the output for **Task 1** and **Task 4**.
4.  Interact with the page elements (inputs, buttons) to verify **Tasks 2, 3, and 5**.

## 🛠 Tech Stack

* HTML
* CSS (Flexbox, Transitions, Figma-based design)
* JavaScript (DOM API, `querySelector`, `addEventListener`, Template literals)
